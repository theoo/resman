%h1 Listing institutes
.listing
  - if @institutes.count > 0
    %table
      %tr
        %th= order_by_link :name
        %th Schools
      - @institutes.each do |institute|
        %tr{:onclick => "window.location='#{edit_institute_path(institute)}'"}
          %td= h institute.name
          %td= link_to "Schools (#{institute.schools.size})", schools_path(q: {institute_id_eq: institute.id})

  - else
    No institutes were found.
  .actions
    = link_to 'New institute', new_institute_path, :class => :fake_button
