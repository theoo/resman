%h1
  Editing Resident
  %em= @resident.full_name
= form_for(@resident) do |f|
  = render :partial => 'form', :locals => { :f => f }
  .actions
    = f.submit 'Update'
    = link_to  'Cancel', :back, :class => "fake_button"
