%h1
  Editing Comment
  %em= @comment.title
= form_for(@comment) do |f|
  = render :partial => 'form', :locals => { :f => f }
  %br/
  .actions
    = f.submit "Update"
    = link_to 'Cancel', :back, :class => "fake_button"
